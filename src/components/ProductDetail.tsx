import { Typography } from "@mui/material";
import { Box } from "@mui/system";
import React from "react";

const ProductDetail = () => {
  return (
    <Box>
      <Typography>Product Detail Page</Typography>
    </Box>
  );
};

export default ProductDetail;
